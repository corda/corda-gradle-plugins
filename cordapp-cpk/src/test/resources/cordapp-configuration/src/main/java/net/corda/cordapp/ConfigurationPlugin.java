package net.corda.cordapp;

import org.gradle.api.Plugin;
import org.gradle.api.Project;

public class ConfigurationPlugin implements Plugin<Project> {
    @Override
    public void apply(Project project) {
    }
}

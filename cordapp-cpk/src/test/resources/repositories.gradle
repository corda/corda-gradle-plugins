repositories {
    mavenCentral()
    maven {
        url "$publicArtifactURL/corda-dependencies"
        mavenContent {
            releasesOnly()
        }
    }
    maven {
        url "$publicArtifactURL/corda-releases"
        mavenContent {
            releasesOnly()
        }
    }
}

plugins {
    id 'java-library'
}

apply from: '../repositories.gradle'
apply from: '../javaTarget.gradle'

jar {
    archiveBaseName = 'library'
}

dependencies {
    // We need "real" Corda here with genuine Maven coordinates.
    implementation "net.corda:corda-core:$corda_release_version"
}

plugins {
    id 'java-library'
}

jar {
    preserveFileTimestamps = false
    reproducibleFileOrder = true
    manifest {
        attributes 'Premain-Class' : 'net.corda.gradle.flask.test.agent.JavaAgent'
    }
}